# Optional: Wird von docker-compose.dev.yml gelesen (Projektroot = Ordner mit docker-compose.dev.yml).
# Nach "docker compose -f docker-compose.dev.yml exec backend python -m scripts.seed"
# die ausgegebene Zeile PUBLIC_SUBMITTER_USER_ID=<id> hier eintragen, dann Backend neu starten.
# PUBLIC_SUBMITTER_USER_ID=5
